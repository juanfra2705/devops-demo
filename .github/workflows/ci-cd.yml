
name: CI/CD Pipeline

on:
  push:
    branches: ["main"]

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: npm install
      - run: npm test

  deploy:
    needs: build-and-test
    runs-on: ubuntu-latest
    steps:
      - name: Simular despliegue
        run: |
          mkdir -p production
          cp src/index.js production/
          echo "✅ App desplegada en entorno de producción:"
          ls -l production
          
